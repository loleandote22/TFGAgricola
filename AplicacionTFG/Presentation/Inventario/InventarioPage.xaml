<Page
    x:Class="AplicacionTFG.Presentation.Inventario.InventarioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplicacionTFG.Presentation.Inventario"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:um="using:Uno.Material"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <!--<Page.DataContext>
        <local:InventarioViewModel />
    </Page.DataContext>-->
    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*,Auto" >
        <TextBlock Text="Inventario" FontSize="40"  VerticalAlignment="Top" Margin="0,10,0,0"/>
        <GridView Grid.Row="1" ItemsSource="{Binding Inventarios}" SelectedItem="{Binding InventarioSeleccionado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <GridView.ItemContainerStyle>
                <Style TargetType="GridViewItem">
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </GridView.ItemContainerStyle>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <utu:Card HeaderContent="{Binding Nombre}" SubHeaderContent="{Binding Descripcion}" AutomationProperties.AutomationId="Elevated_Card" Style="{StaticResource ElevatedCardStyle}" Height="200" Width="300" />
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        <local:ElementoControl x:Name="ElementoControl" Grid.RowSpan="2" Grid.Column="1" Visibility="{Binding ElementoControlVisibility}" />   
    </Grid>
</Page>
