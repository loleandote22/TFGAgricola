<Page
    x:Class="AplicacionTFG.Presentation.Inventario.ElementoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AplicacionTFG.Presentation.Inventario"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" ColumnDefinitions="*,Auto" RowSpacing="16" >
        <Button Content="{Binding Editar_Loc}" Command="{Binding EditarCommand}" Margin="10"/>
        <Button Content="{Binding Eliminar_Loc}" Grid.Column="1" Command="{Binding EliminarCommand}"/>
        <TextBlock Text="{Binding Elemento.Nombre}" FontSize="40" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top"/>
        <TextBlock Text="{Binding Elemento.Cantidad}" FontSize="20" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top" />
        <TextBlock Text="{Binding Elemento.Descripcion}" MinWidth="300" Height="150" FontSize="20" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" />
        <TextBlock Text="{Binding Elemento.Tipo}" HorizontalAlignment="Center" Grid.Row="3" />
        <Grid Grid.Row="4" Grid.ColumnSpan="2">
            <ScrollView VerticalScrollMode="Auto" >
                <ListView ItemsSource="{Binding Elemento.InventarioEventos}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <TextBlock Text="{Binding Fecha}" />
                                <TextBlock Text="{Binding Tipo}" />
                                <TextBlock Text="{Binding Cantidad}" />
                                <TextBlock Text="{Binding UsuarioNombre}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollView>
        </Grid>
    </Grid>
</Page>
